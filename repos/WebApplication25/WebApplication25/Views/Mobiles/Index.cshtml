<style>
    .top{
        color:White;
        background-color:mediumaquamarine;
        border-radius:50px;
        padding:10px;
        margin:10px
    }
    .image{
        margin:15px;
        height:200px;
        width:250px
    }

    .image{
       align-content:center
    }
    table, th, td {
        margin:10px;
        border-spacing:10px;
        text-align:center;
       font-weight:bold
    }

    .topsearch {
        color: White;
        background-color: blue;
        border-radius: 50px;
        padding: 10px;
        margin: 10px
    }

    form button {
        color: white;
        background-color: green
    }
</style>


@model IEnumerable<WebApplication25.Models.Mobile>

@{
    ViewData["Title"] = "Index";
}
<div class="container">
    <div class="top">
        <Center>
            <h1>Mobile Best Sellers</h1>
            <h5>Our most popular products based on sales. Updated hourly.</h5>
        </Center>
    </div>

    <div class="container">
        <div class="topsearch">
            <Center>
                <form asp-action="Details" method="POST">
                    <input type="search" name="search" id="search" placeholder="Search Product" />
                    <input type="submit" value="Search" />
                </form>
            </Center>
        </div>
    </div>

@*<p>
    <a asp-action="Create">Create New</a>
</p>*@
@*<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Images)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>*@

<table>
    <thead>
@foreach (var item in Model) {   
        <tr>
            <td>
                    <image src=@Html.DisplayFor(modelItem => item.Images)
                   style="margin:15px;height:300px;width:200px"></image>
                    </td>
                   <td>
                        <h1 asp-action="Details" method="POST">@Html.DisplayFor(modelItem => item.Name)</h1>
                    
                        <h4 style="color:deepskyblue">Price : ₹ @Html.DisplayFor(modelItem => item.Price)</h4>
            </td>

           @* <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>*@
        </tr>
}
    </tbody>
</table>
